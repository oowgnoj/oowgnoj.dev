import React from 'react';
import Layout from '../layout';
import SEO from '../layout/seo';

export default () => {
    return (
        <Layout>
            <SEO title="about" />
        </Layout>
    );
};
